# Development and testing dependencies — NOT installed in the Docker image.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.0
aiosqlite==0.20.0
